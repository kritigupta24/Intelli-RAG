# Intelli-RAG
Run main.py

creat a file .env and give groq api key

pip install -r requirements.txt

streamlit run app_main.py

--------------------------------------------------
Engineered a Retrieval-Augmented Generation (RAG) chatbot leveraging LangChain, FAISS vector search, and Groq
LLMs to enable low-latency, context-aware responses.
• Integrated real-time text/voice interaction, dynamic PDF retrieval with contextual compression, and deployed a
scalable Streamlit interface for efficient virtual assistance.
